version: '3.8'

services:
  # Example application using the public SDK contract.
  example-agent-service:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - MULTINEX_API_ENDPOINT=http://multinex-api-stub:8080/v1
      - MULTINEX_API_KEY=mnx_sk_test_demo
      - AGENT_NAME=model-router
